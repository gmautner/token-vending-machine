# IMPORTANT: Replace with actual credentials before applying
# Consider using external-secrets, sealed-secrets, or similar for production
apiVersion: v1
kind: Secret
metadata:
  name: token-vending-machine-aws
  namespace: token-vending-machine
type: Opaque
stringData:
  AWS_ACCESS_KEY_ID: "REPLACE_ME"
  AWS_SECRET_ACCESS_KEY: "REPLACE_ME"

